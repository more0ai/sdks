# Common SDKs

Shared types, pipeline, middleware, policy, wire protocol, bootstrap, and worker assignments.

| Language | Path    | Package / install            |
|----------|---------|------------------------------|
| Node/TS  | `node/` | `@more0ai/common` (pnpm)     |
| PHP      | `php/`  | `more0ai/common` (Composer)  |

See each language directory for usage.
