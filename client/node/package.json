{
  "name": "@more0ai/client",
  "version": "0.1.1",
  "repository": "github:more0ai/sdks",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "description": "Single entry point for more0ai interaction — client for invoking and discovering more0ai via NATS",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@more0ai/common": "workspace:*",
    "nats": "^2.29.3"
  },
  "peerDependencies": {
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
