{
  "name": "sdks",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for @more0ai/core, @more0ai/client, @more0ai/worker",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck --if-present"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
