# ─── Comms (transport) ─────────────────────────────────────────────────────
# Server URL for the messaging transport
COMMS_URL=nats://127.0.0.1:4222
# Name for this process (shows up in connection metadata / logs)
SERVICE_NAME=capabilities-worker

# ─── Config files ──────────────────────────────────────────────────────────  
# Path to worker config JSON: sandboxes + workerPools (required for sandbox/pool setup)
CONFIG_PATH=./config.example.json
# Optional: path to bootstrap JSON for capability -> subject resolution (omit to use built-in default)
BOOTSTRAP_PATH=
