<?php

declare(strict_types=1);

namespace More0ai\Worker;

/**
 * Worker pool for capability invocations with consumer-group subscription.
 *
 * Placeholder. Implement NATS subscription, envelope validation (via more0ai/core),
 * and handler registry to mirror the Node SDK.
 */
final class Worker
{
    public function __construct()
    {
    }
}
